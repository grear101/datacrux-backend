# Database
DATABASE_URL="postgresql://user:password@localhost:5432/datacrux?schema=public"

# Phase 1 is single-tenant: one hardcoded active client until the Auth
# Service and real multi-tenant request routing exist (Phase 2).
ACTIVE_CLIENT_ID="replace-with-real-client-id-after-seeding"

PORT=3000
